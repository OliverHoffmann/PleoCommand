<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suppressions PUBLIC
    "-//Puppy Crawl//DTD Suppressions 1.1//EN"
    "http://www.puppycrawl.com/dtds/suppressions_1_1.dtd">

<suppressions>

	<!-- Needed there because it's the only way to print error during app exit -->
	<suppress id="SystemPrintCheck" files="GUIFrame.java" lines="380-400" />

	<!-- This is the only class where data gets written to stdout/stderr -->
	<suppress id="SystemPrintCheck" files="Log.java" />

	<!-- This is the only class where data gets read from stdin -->
	<suppress id="SystemPrintCheck" files="StandardInput.java" />

	<!-- Need to catch Throwable at end of thread -->
	<suppress checks="IllegalCatch" files="MainLogPanel.java" lines="100-150" />

	<!-- Catch everything that may go wrong here -->
	<suppress checks="IllegalCatch" files="Value.java" lines="40-100" />

	<!-- Need to declare Throwable in finalize() -->
	<suppress checks="NoFinalizer" files="PipePart.java" lines="110-130" />
	<suppress checks="IllegalThrows" files="PipePart.java" lines="110-130" />

	<!-- Contains only GUI component creation -->
	<suppress checks="ExecutableStatementCount"
	          files="DataSequenceEditorFrame.java" />

	<!-- The class this one relies on are mainly GUI components -->
	<suppress checks="ClassFanOutComplexity"
	          files="DataSequenceEditorFrame.java" />

	<!-- All as...() converter methods need to be overridable -->
	<suppress checks="DesignForExtension" files="Value.java" lines="15-35" />

	<!-- Optimized for speed and simpleness, so needs to modify i and j 
	 inside hex conversion code -->
	<suppress checks="ModifiedControlVariable"
	          files="Data.java"
	          lines="200-260" />

	<!-- Well documented -->
	<suppress checks="ModifiedControlVariable"
	          files="Pipe.java"
	          lines="110-180" />

</suppressions>